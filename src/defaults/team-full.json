{
  "name": "full",
  "verifiers": {
    "testers": ["tester"],
    "browser_testers": ["tester_browser"],
    "reviewers": ["architect"]
  },
  "agents": {
    "worker_fast": {
      "backend": "opencode",
      "model": "gemini-2.5-flash",
      "description": "Fast coding agent — use for straightforward tasks where speed matters.\nDirective: 1-3 sentences describing desired BEHAVIOR, not implementation.\nIf stuck, set new_conversation=true with a fresh directive.\nEach task: ONE independently testable feature or change.",
      "max_turns": 30,
      "timeout_s": 1800
    },
    "worker_smart": {
      "backend": "opencode",
      "model": "gemini-2.5-flash",
      "description": "Powerful reasoning agent — use for complex tasks, debugging, or when the fast worker struggled.\nDirective: 1-3 sentences describing desired BEHAVIOR, not implementation.\nIf stuck, set new_conversation=true with a fresh directive.\nEach task: ONE independently testable feature or change.\nIf result contains [PROPOSED PLAN], approve or request changes.",
      "max_turns": 30,
      "timeout_s": 1800
    },
    "frontend_worker": {
      "backend": "claude",
      "model": "claude-sonnet-4-6",
      "description": "Frontend-focused implementation agent — use for UI, interaction, styling, and client-side behavior.\nDirective: 1-3 sentences describing desired BEHAVIOR, not implementation.\nEach task: ONE independently testable frontend change.",
      "max_turns": 30,
      "timeout_s": 1800
    },
    "backend_worker": {
      "backend": "codex",
      "model": "gpt-5.3-codex-spark",
      "description": "Backend-focused implementation agent — use for APIs, data flow, server logic, and infrastructure-facing code.\nDirective: 1-3 sentences describing desired BEHAVIOR, not implementation.\nEach task: ONE independently testable backend change.",
      "max_turns": 30,
      "timeout_s": 1800
    },
    "tester": {
      "backend": "opencode",
      "model": "gemini-2.5-flash",
      "description": "Verifies features end-to-end. Give it a user-experience description to check.\nReports what works and what's broken. Does not fix anything.",
      "max_turns": 20,
      "timeout_s": 1800
    },
    "tester_browser": {
      "backend": "opencode",
      "model": "gemini-2.5-flash",
      "description": "Tester with real browser access — use for web UI verification.\nReports issues but does not fix anything.",
      "max_turns": 20,
      "timeout_s": 1800,
      "chrome": true
    },
    "architect": {
      "backend": "opencode",
      "model": "gemini-2.5-flash",
      "description": "Code reviewer. Focuses on key decisions and structural issues.\nDoes not implement features.",
      "max_turns": 10,
      "timeout_s": 600
    }
  }
}
